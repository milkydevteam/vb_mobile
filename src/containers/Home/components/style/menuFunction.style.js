import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  container: {
    marginTop: 7,
    height: 100,
    width: '100%',
  },
  contentStyle: {
    padding: 10,
  },
});

export default style;
